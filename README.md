# mpvd

MPV music player daemon controller CLI.

```sh
npm i -g mpvd
```

> Note: `mpv` is required.

Print usage info.

```sh
mpvd help
```

# mpvctl

Requires `socat`, `jq`, `fzf` and `bat`.

```sh
mpvctl help
```
